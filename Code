<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pirate Blog</title>
  <style>
    body {
      margin: 0;
      font-family: 'Georgia', serif;
      background: #f4f1e1;
    }

    #blog {
      padding: 40px;
      max-width: 700px;
      margin: auto;
    }

    .post {
      border: 2px solid #222;
      padding: 20px;
      background: #fff;
      cursor: pointer;
    }

    .post:hover {
      background: #eee;
    }

    #ship-scene {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: url('https://i.imgur.com/QGHFzvR.jpg') no-repeat center center / cover;
      display: none;
      justify-content: center;
      align-items: flex-end;
      z-index: 10;
      transition: opacity 2s ease;
    }

    #blackout {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      background: black;
      opacity: 0;
      transition: opacity 3s ease;
    }

    #seabed {
      display: none;
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      background: url('https://i.imgur.com/k7vDLc7.jpg') no-repeat center center / cover;
      z-index: 20;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-end;
      color: white;
      padding-bottom: 60px;
    }

    .skeletons {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-bottom: 30px;
    }

    .skeletons img {
      width: 100px;
    }

    #question {
      font-size: 28px;
      text-align: center;
      margin-bottom: 20px;
    }

    #form {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
    }

    #form input, #form textarea {
      padding: 10px;
      width: 300px;
      font-size: 16px;
    }

    #form button {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
    }

    #credits {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 100%;
      background: url('https://i.imgur.com/k7vDLc7.jpg') repeat-y center top / cover;
      z-index: 30;
      overflow: hidden;
      color: white;
      font-size: 20px;
      display: flex;
      justify-content: center;
    }

    #credit-list {
      position: absolute;
      bottom: -100%;
      text-align: center;
      animation: scrollUp 60s linear infinite;
    }

    @keyframes scrollUp {
      0% { bottom: -100%; }
      100% { bottom: 100%; }
    }
  </style>
</head>
<body>

<div id="blog">
  <h1>Captain's Blog</h1>
  <div class="post" id="shipPost">
    <h2>🌊 Our ship sails again!</h2>
    <p>We've set out across the eastern sea with treasures untold. May the waves guide us true.</p>
  </div>
</div>

<div id="ship-scene">
  <div id="blackout"></div>
</div>

<div id="seabed">
  <div class="skeletons">
    <img src="https://i.imgur.com/POULMC2.png" alt="Skeleton">
    <img src="https://i.imgur.com/POULMC2.png" alt="Skeleton">
    <img src="https://i.imgur.com/POULMC2.png" alt="Skeleton">
  </div>
  <div id="question">What did we even live for?</div>
  <form id="form">
    <input type="text" placeholder="Your name" required>
    <textarea placeholder="Your final words..." required></textarea>
    <button type="submit">Submit</button>
  </form>
</div>

<div id="credits">
  <div id="credit-list">
    <p>⚓ Jack: I wanted more than gold.</p>
    <p>⚓ Anne: We laughed at the storm, but the storm laughed back.</p>
    <p>⚓ Morgan: I just wanted to be remembered.</p>
    <p>⚓ Drake: Why did we follow the horizon?</p>
    <p>⚓ Calico: The sea keeps no promises.</p>
    <p>⚓ Your name here...</p>
  </div>
</div>

<script>
  const post = document.getElementById('shipPost');
  const shipScene = document.getElementById('ship-scene');
  const blackout = document.getElementById('blackout');
  const seabed = document.getElementById('seabed');
  const form = document.getElementById('form');
  const credits = document.getElementById('credits');

  post.addEventListener('click', () => {
    shipScene.style.display = 'flex';
    setTimeout(() => {
      blackout.style.opacity = 1;
      setTimeout(() => {
        shipScene.style.display = 'none';
        seabed.style.display = 'flex';
      }, 3000);
    }, 1500);
  });

  form.addEventListener('submit', (e) => {
    e.preventDefault();
    seabed.style.display = 'none';
    credits.style.display = 'flex';
  });
</script>

</body>
</html>
